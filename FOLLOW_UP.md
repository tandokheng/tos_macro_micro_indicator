# Follow Up

## High Leverage

- FU-001 - Replace `_dk_codex_macro_micro_v1` with `MacroMicro_Simplified_v0.5.7.ts` in Thinkorswim and confirm it compiles.
- FU-002 - Confirm whether the magenta/cyan big arrows remain visible now that failsafe bubbles default off and setup pulses are throttled.

## Easy To Complete

- FU-003 - Confirm the dashboard shows `PROFILE: 5m` and `SETUP` displays as `4/6` instead of `4.00/6`.
- FU-004 - Screenshot one quieter v0.5.7 setup area so we can judge whether `setupPulseBars = 8` is too sparse or still too busy.
- FU-005 - After 5-minute review, collect a smaller QQQ 15-minute sample for separate validation.

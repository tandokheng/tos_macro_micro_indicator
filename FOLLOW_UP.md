# Follow Up

## High Leverage

- FU-001 - Replace `_dk_codex_macro_micro_v1` with `MacroMicro_Simplified_v0.5.47.ts` and confirm `BUILD: v0.5.47 NQ REVIEW`.
- FU-002 - Mark one `/NQ` 5m trend where PSAR stays aligned after TP2, then confirm the plan shows `RUN PSAR` instead of ending too early.

## Easy To Complete

- FU-003 - Screenshot one `/NQ` chop area to confirm 4/6 ready dots stay side-locked instead of alternating.
- FU-004 - Confirm `R-INV` appears once when a review condition invalidates without TP2 or stop ending the plan.
- FU-005 - For `/NQ` options validation, capture the exact option contract: strike, expiry, call/put, bid/ask, delta, and spread before estimating dollar P/L.

# Follow Up

## High Leverage

- FU-001 - Replace `_dk_codex_macro_micro_v1` with `MacroMicro_Simplified_v0.6.0.ts` and confirm `BUILD: v0.6.0 CLEAN`.
- FU-002 - Compare one `/NQ` 5m clean trend using three studies: repainting original, `Original_Comparison_NonRepainting_v1.ts`, and `MacroMicro_Simplified_v0.6.0.ts`.

## Easy To Complete

- FU-003 - Screenshot one `/NQ` chop area to confirm ready dots stay side-locked instead of alternating.
- FU-004 - Confirm `R-INV` appears once when a macro plan invalidates without TP2 or stop ending the plan.
- FU-005 - For `/NQ` options validation, capture the exact option contract: strike, expiry, call/put, bid/ask, delta, and spread before estimating dollar P/L.

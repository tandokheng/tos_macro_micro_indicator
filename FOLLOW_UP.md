# Follow Up

## High Leverage

- FU-001 - Replace `_dk_codex_macro_micro_v1` with `MacroMicro_Simplified_v0.5.41.ts` in Thinkorswim and confirm it compiles with `BUILD: v0.5.41 REVIEW RISK`.
- FU-002 - Capture one chop screenshot where `R-TP` / `R-SL` shows whether a remaining arrow would stop out quickly or had enough room to work.

## Easy To Complete

- FU-003 - Confirm each visible magenta/cyan review arrow gets review-only `R-TP` and `R-SL` context without changing the real PT/SL bubbles.
- FU-004 - If the chart feels cluttered, first set `showReviewTargetStopBubbles = no` while leaving `showReviewTargetStopLines = yes`.
- FU-005 - Capture one trend/reversal screenshot where `R-TP` looks plausible, so chop fixes do not overfit one bad window.

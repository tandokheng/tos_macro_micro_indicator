# v0.5.47 NQ Review Management Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build `MacroMicro_Simplified_v0.5.47.ts` from v0.5.46 with side-locked ready dots, clearer review-plan invalidation, and PSAR-aware review management for `/NQ` 5m validation.

**Architecture:** Keep the existing single ThinkScript study structure and add scoped review-state variables rather than changing the real trade engine. Use static verifier checks to guard the ThinkScript contract, then sync the import-ready and readable copies exactly.

**Tech Stack:** ThinkScript source files, PowerShell verifier, Git.

---

### Task 1: Failing Verifier

**Files:**
- Modify: `tests/verify_simplified_indicator.ps1`

- [x] **Step 1: Write failing checks**

Add checks that expect `MacroMicro_Simplified_v0.5.47.ts`, `MacroMicro_Simplified_v0.5.47_READABLE.txt`, build labels for v0.5.47, explicit side-lock ready-dot state, review invalidation bubble controls, and PSAR runner/gradient variables.

- [x] **Step 2: Run verifier to verify it fails**

Run: `powershell -NoProfile -ExecutionPolicy Bypass -File tests\verify_simplified_indicator.ps1`
Expected: FAIL before the v0.5.47 study exists.

### Task 2: v0.5.47 Study

**Files:**
- Create: `MacroMicro_Simplified_v0.5.47.ts`
- Modify: `_dk_codex_macro_micro_v1.ts`
- Create: `MacroMicro_Simplified_v0.5.47_READABLE.txt`

- [x] **Step 1: Copy v0.5.46 as the baseline**

Copy `MacroMicro_Simplified_v0.5.46.ts` to `MacroMicro_Simplified_v0.5.47.ts`.

- [x] **Step 2: Implement scoped ThinkScript changes**

Update header/build strings, replace the implicit setup-campaign latch with explicit `setupReadyLockedDir`, add optional `showReviewInvalidationBubble`, add PSAR slope/gradient review variables, let TP2 become a runner state while PSAR remains aligned, and keep hard stop logic intact.

- [x] **Step 3: Sync import/readable files**

Copy the final v0.5.47 source into `_dk_codex_macro_micro_v1.ts` and `MacroMicro_Simplified_v0.5.47_READABLE.txt`.

### Task 3: Project Notes And Checkpoint

**Files:**
- Modify: `VERSIONING.md`
- Modify: `NEXT_TASKS.md`
- Modify: `FOLLOW_UP.md`
- Modify: `DECISION_LOG.md`

- [x] **Step 1: Record the build**

Document the v0.5.47 behavior and move v0.5.46 installation work out of active status.

- [x] **Step 2: Verify**

Run: `powershell -NoProfile -ExecutionPolicy Bypass -File tests\verify_simplified_indicator.ps1`
Expected: `Simplified indicator verification passed.`

- [ ] **Step 3: Commit and push**

Run: `git status --short`, stage the scoped files, commit with `Add NQ review management build`, and push `main` to `origin`.
